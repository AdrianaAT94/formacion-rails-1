<div class="container">
  <h1 class="title is-1">Modificar talla del producto <%= @product.name %> de la tienda <%= @store.name %></</h1>
</div>

<div class="container">
  <%= link_to 'Volver', store_product_size_path(@store, @product, @size), class: 'button is-primary' %>
  <%= render 'form' %>
</div>