<div class="container">
  <h1 class="title is-1">Tallas del producto <%= @product.name %></h1>
</div>

<div class="container">
  <%= link_to 'Volver a producto', product_path(@product), class: 'button is-primary' %>
  <%= link_to 'Nueva talla', new_product_size_path(@product), class: 'button is-primary' %>
  <%= render 'products/sizes' %>
</div>