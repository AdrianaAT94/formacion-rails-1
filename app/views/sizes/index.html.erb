<div class="container">
  <h1 class="title is-1">Tallas del producto <%= @product.name %> de <%= @store.name %></h1>
</div>

<div class="container">
  <%= link_to 'Volver', store_product_path(@store, @product), class: 'button is-primary' %>
  <%= link_to 'Nueva talla', new_store_product_size_path(@store, @product), class: 'button is-primary' %>
  <%= render 'products/sizes' %>
</div>